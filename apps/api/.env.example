API_PORT=8000
DATABASE_URL=
AWS_REGION=us-east-1
S3_BUCKET=adhocprintstudio-dev-files
SQS_QUEUE_URL=

# AI features (optional) — set one or both providers, then restart the API.
# Enables the Anthropic provider.
#ANTHROPIC_API_KEY=
# API key for OpenAI or a compatible endpoint (optional for local servers).
#OPENAI_API_KEY=
# Enables the OpenAI-compatible provider; point at OpenAI, or at a local
# Ollama/vLLM/LM Studio server to keep data on-network.
#OPENAI_BASE_URL=
